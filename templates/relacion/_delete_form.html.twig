<form method="post" action="{{ path('app_relacion_delete', {'id': relacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relacion.id) }}">
    <button class="btn">Delete</button>
</form>
